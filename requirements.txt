# Direct dependencies with compatible ranges. CI installs the exact versions
# in requirements.lock; regenerate it with:
#   pip install -U -r requirements.txt && pip freeze > requirements.lock  (in a clean venv)
anthropic>=1.8,<2
pydantic>=2.13,<3
requests>=2.31,<3
beautifulsoup4>=4.12,<5
jinja2>=3.1,<4
rich>=13.0
